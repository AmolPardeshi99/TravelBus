<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.travelbus">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TravelBus">
        <activity
            android:name=".views.adapter.activities.PaymentSuccessActivity"
            android:exported="true" />
        <activity
            android:name=".views.adapter.activities.BusBookingActivity"
            android:exported="true"></activity>
        <activity
            android:name=".views.adapter.activities.OnBoardingActivity"
            android:exported="true" />
        <activity
            android:name=".views.adapter.activities.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.adapter.activities.HomeActivity"
            android:exported="true"></activity>
        <activity
            android:name=".views.adapter.activities.MainActivity"
            android:exported="true" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>